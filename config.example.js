/*
  Copy this file to `config.js` and add your OpenWeatherMap API key.
  `config.js` is gitignored, so your key is never committed.

  NOTE: This is a static, client-side site. The key is still visible to
  visitors in their browser (Network tab / view-source) at runtime — there
  is no way to fully hide a client-side key. Protect it by restricting the
  key to your site's domain in your OpenWeatherMap account settings.
*/
window.WEATHER_API_KEY = "YOUR_OPENWEATHERMAP_API_KEY";
